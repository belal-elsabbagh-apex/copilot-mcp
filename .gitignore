node_modules/
bun.lock
# build output (regenerated by `bun run build`; shipped in the npm tarball via "files")
dist/
# secrets / local config — never commit
.local/
config.local.json
*.env
.env*
# debug artifacts from the order mirror
prod-order-*.json
# os
.DS_Store
*.log
